apiVersion: v1
kind: Secret
metadata:
  name: cloud-credentials
  namespace: velero
type: Opaque
stringData:
  cloud: |
    [default]
    aws_access_key_id = {{ velero_user_key_id }}
    aws_secret_access_key = {{ velero_user_key }}
